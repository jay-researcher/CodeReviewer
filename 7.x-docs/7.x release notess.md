# CodeReviewer 7.x Release Notes

> 本文档记录 CodeReviewer 7.x 每次发布的功能、行为变化、配置变化、验证结果和部署状态。
>
> 维护规则：后续每次 7.x 版本更新，都必须同步更新本文档和《CodeReviewer User Mannual》，并将同一发布提交推送到配置的远程代码仓库。发布结果必须记录版本号、分支、commit 和 push 状态；push 失败时不得宣称发布完成。未明确执行生产部署时，Release Notes 必须标记为“仅本地”，不得暗示已经部署到 `192.168.3.78`。

## 7.2.13 — 2026-07-19

发布状态：仅本地环境 `127.0.0.1:8765`；未访问、部署或重启 `192.168.3.78:8765`。

- Jira Review 在 LLM 分析前按 `Application + Release Line` 切分 MR 输入；同范围多个 MR 合并，不同应用/版本线分别生成报告、Finding、Handling 与 Snapshot。
- 新报告名支持 `WVAdmin`、`Services-Terminal`、`iTrade-Client-7.5.0`、`iTrade-Client-7.5.1`、`DPS9`、`DPS11`；iTrade 同 repository URL 结合 source/target branch 判定版本线。
- Sprint Overview 和 Issues Review History 按版本线展示 Release Readiness；Issue 详情按最新逻辑 Run Group 汇总多个应用报告及 prompt chunks，Problem 卡显示所属范围。
- Auditor/Developer 可见性使用同一 Issue 全部 Review Run 的 `responsible_scope` 并集；旧 Sprint 报告也读取结构化范围授权，Manager 创建的报告可由对应 Auditor 继续处理。
- Company Config/SCR 在普通 Review 前继续延后，并只向匹配应用/版本线的 Release Gate 范围提供证据，避免跨应用文件清单污染。
- Workflow SQLite schema 升级到 v3，旧数据库幂等补列且保留 Run/Cycle 数据；报告 metadata 只读取有界头部，避免历史列表扫描大文件。
- 配置页面保留 `application`、`release_line(s)`；完整自动化测试 `252/252` 通过。本版本只更新本机。

## 7.2.12 — 2026-07-19 12:31:03 CST

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview 打开时不再把主页 Jira 字段回填到 Jira Issues；扫描结果以 `Overview` 和 `Sprint issues` 两个 Tab 分开，减少报告覆盖、应用准备度与逐 Issue 操作的视觉拥挤。
- Issues Review History Overview 的应用卡改为桌面三列，并提高指标、应用名称和统计文字的字号；窄屏继续降级为两列或一列。
- Problems 从 Finding 的结构化 `detail/problem` 与 `recommendation/suggestion` 分别展示“问题”和“建议”，每项最多两行，并用 `更多` / `收起` 展开完整证据。
- Review Communication 使用 XL 弹窗，扩大可用宽高并重新平衡 History、Follow-up Draft、Reply 三栏；Reply/Follow-up 卡片共享可用高度，按钮保持正常文档流。
- User Management 左侧账户列表扩大到约 42%，提升账户、scope 字号，加载后默认打开第一条记录；Configuration 将 `LLM` 保持为标准大写缩写。
- 定义 S、M、L、XL、Full 五档弹窗尺寸 Token，并应用到确认、Release Notes、报告预览、Sprint Overview、Review Communication 和管理工作区。
- 新增 7.2.12 UI 反馈契约测试；Python 完整测试 `238/238`、编译和渲染后内联 JavaScript 语法检查通过，Git whitespace 在发布提交前复核。本版本只更新本机。

## 7.2.11 — 2026-07-19 01:14:17 CST

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- 单 Jira Review 在创建 Job 前增加 Progress preflight 卡片，分阶段显示已有报告检查、freshness 判断、等待用户决策和创建 Job；Cancel、Use Existing、失败与正常启动都会清理临时状态。已有报告确认弹窗补齐焦点约束、Escape 和焦点恢复。
- Release Notes 的 `Failed to fetch` 根因是部分 Windows/RHEL Python 环境缺少 IANA `tzdata`，`ZoneInfo("Asia/Shanghai")` 会中断 HTTP 响应。现在使用不依赖系统时区包的固定 UTC+8，文件缺失、读取失败仍返回结构化内容；客户端增加一次短退避自动重试、恢复卡、手动 Retry 和会话内缓存。
- User Management 列表明确显示 `Responsible · identifier`，Manager 显示 `Global scope`；Configuration 分类增加独立 surface、标题间距和安全内边距；Sprint Overview 使用紧凑 footer 和 `View sprint issues`。
- Report Review 的 Problems 默认显示标题、Problem 与 Suggestion 两行摘要，使用 More/Less 查看完整证据；各 Tab 增加一致的四边内容间距。Review Communication 的 Reply/Follow-up 改为同构卡片、文档流 CTA，并在 961–1180px 自动重排。
- Jira 涉及文件清单差异按结构化 `deferred_release_gate_resources` 建立文件到来源类型映射，报告明确写出 `Company Config` 或 `SCR`；旧元数据无法确定时使用 `Deferred resource (type unavailable)`，前端不按扩展名猜测。
- `config.yml` 的 `branch`/`branches` 同时支持精确分支与版本通配符；每个通配符从远端 heads 按数字版本选择最高分支，无匹配时返回可诊断配置错误。iTrade Client、DPS、WVAdmin、Services Terminal 已使用相应版本线模式。
- 知识上下文策略调整为 Jira REST 权威数据及唯一写入渠道、Rovo MCP 只读候选上下文；本地 `jira-prd`/RAG 默认关闭。Rovo 缺少凭据、适配器或暂时不可用时不阻断代码审查。
- 采用三个独立 Agent 对 UI/UX、分支/Rovo 后端和 QA 进行交叉审查；新增真实 Release Notes HTTP、分支模式、知识提供者策略、特殊 MR 来源和反馈 UI 契约测试。Python 全量自动化测试 `232/232`、编译、Effective Config 解析和内联 JavaScript 语法检查通过。本版本只更新本机。

## 7.2.10 — 2026-07-19 00:40:11 CST

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview Coverage 从同步长请求改为独立后台 Job。创建接口立即返回 Job ID，前端通过状态接口轮询，因此 Jira/GitLab Discovery 超过 60 秒时不再被浏览器主动中断。
- 同一用户、同一 Jira/Sprint/Filter 范围只运行一个 Coverage Job；进行中的重复提交直接复用原任务，刚完成的结果短期复用，降低 Jira/GitLab 重复访问压力。
- Coverage Job 按用户隔离，保留实际阶段、Issue 进度、完成百分比、事件、结果及错误；后台任务在关闭弹窗后继续运行，重新打开或页面刷新后可通过本地 Job ID 恢复。
- 进度氛围分为常规扫描和长时间扫描。前 30 秒显示实际阶段和用时；30 秒后切换为强调色状态，并显示到 180 秒阈值的超时倒计时。达到阈值后明确提示任务仍在后台继续，不再误报网络中断或丢弃结果。
- MR Discovery 为每个 Jira Issue 发送真实进度事件；报告匹配、Review handling、Review Cycle 和应用 Release Readiness 计算也分别展示阶段。
- 新增后台完成、HTTP 创建/轮询、同范围去重、短期结果缓存、权限隔离、恢复机制和前端倒计时契约测试；Python 全量自动化测试 `220/220`、编译及内联 JavaScript 语法检查通过。本版本只更新本机。

## 7.2.9 — 2026-07-18 14:33:16 CST

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- 新增 Manager `Configuration` 工作区。应用设置和 GitLab 项目元数据均从 Effective Config 读取，支持编辑及在现有 Group 下新增/删除项目子节点；Web 修改写入独立 override，不直接重写 `config.yml`。写入具备 revision 乐观锁、跨进程锁、原子替换、保存前备份、脱敏审计和一键恢复。
- GitLab 项目增加显式 `application` 归属，支持 WVAdmin、iTrade Client、Services Terminal、DPS 和 Unmapped；新 Review Run 将应用归属持久化到当前 Review Cycle。
- 登录页和主页新增 `Healthy / Degraded / Unavailable` 状态。未登录摘要保持脱敏，登录后详情包含配置、数据、报告、工作流及可选外部服务检查。
- User Management 左栏改为稳定双行卡片与分层筛选；Responsible mapping 明确为访问范围而不是上下级关系，Developer/Auditor 必填，Manager 显示 Global access。
- Change Password 和 Ask AI 的必填星号统一紧跟字段名；Review Communication 增加 Jira/报告标题、Handling、Critical/High remaining 和 Pass readiness 上下文，Reply/Follow-up 卡片等高且按钮保持正常文档流。
- Release Notes 显示文件最后更新时间；报告正文调整为 15px、1.68 行高并增加段落/列表节奏。
- Issues Review History > Overview 按 Sprint 展示四个应用的 Reports、Without report、Generating、Handling、Ready for Pass、Review Pass、Failed、Remaining 和百分比；计算读取当前 Cycle 的应用级 Run，旧 Sprint 不再混入当前进度，无法可靠归属的记录进入 Unmapped。
- Multi-Agent 进行了 UI、配置安全和验收矩阵交叉审查；新增 Effective Config、应用级 Cycle 进度和反馈契约测试。本版本只更新本机。

## 7.2.8 — 2026-07-18

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview 新增 `Application release readiness`，基于本次扫描发现的 Jira Issues 与 MR 项目归属，分别汇总 WVAdmin、iTrade Client、Services Terminal、DPS 的 Review 进度。
- 每个应用以 `Review Pass Issues / Scoped Issues` 计算发布准备百分比；有报告但仍在 Handling 或 Ready for Pass 的 Issue 不会被误算为可发布。达到 100% 只表示可进入该应用的 GIT_VERSION Release Gate，最终门禁仍需验证锁定源码和发布资源。
- 应用卡片同时展示 `Reports / Without report` 覆盖情况，以及 `Generating`、`Handling`、`Ready`、`Pass`、`Failed` 数量、报告覆盖率和剩余待处理 Issue 数。
- 应用归属优先读取 `config.yml` 的 git-tools group/module/project 元数据；跨应用 Issue 在每个相关应用中各计一次。无法可靠映射的记录进入 `Unmapped` 并保持门禁阻断，禁止按标题猜测或静默忽略。
- Sprint Issue 卡片增加所属应用标签，便于从全局进度下钻核对具体 Issue。
- 新增项目映射、跨应用去重、百分比和门禁准备状态测试；Python 全量自动化测试 `189/189` 与内联 JavaScript 语法检查通过。本版本只更新本地验收环境。

## 7.2.7 — 2026-07-18

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview 汇总改为两层信息架构：第一层对比 `Issues with reports` 与 `Issues without reports`；数量按 Jira Issue 去重，不把同一 Issue 的多个历史报告重复计数。
- 已生成报告的 Issues 按最新报告/Review Cycle 状态细分为 `Handling`、`Ready for Pass`、`Review Pass` 三类。
- `Generating` 和 `Failed` 作为报告生成过程/异常独立显示，不错误归入“已生成报告”三分类。
- 主对比卡增加数量、占比和氛围进度条；生命周期卡展示三个细分类及运行状态。
- 每张 Issue 卡片补充当前 Cycle、Latest Run、Sprint 和 Snapshot 数量；尚无工作流记录时明确显示 `No Review Cycle yet`。
- 新增按 Issue 报告覆盖与三种生命周期状态的后端专项测试；全量自动化测试 `187/187` 通过。

## 7.2.6 — 2026-07-18

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Coverage Scan 增加 60 秒客户端超时和完整状态复位；网络中断时不再永久停留在 `Preparing overview`，恢复连接后可以重新 Scan。
- 汇总指标 `No report` 改为 `Issues without report`，明确数值表示“缺少报告的 Issue 数量”，而不是报告数量。
- Issue 卡片右上状态区纵向编排操作与 Jira 状态；未生成报告时显示 `Run Review`，`Development Done` 等 Jira 状态显示在按钮下方。
- 卡片 `Run Review` 不再关闭 Sprint Overview；提交后在弹窗内重新 Scan，卡片切换为 Generating，同时主页面 Progress 继续保留完整 Job。
- Review Progress 在滚轮、拖动/点击垂直滚动条、触摸或键盘滚动后暂停自动跟随；用户静置 60 秒后恢复并跳转到最新消息，`Jump latest` 可立即恢复。
- Job event stream 增加键盘焦点与可访问名称。

## 7.2.5 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview 结果区从传统表格改为真正的响应式 Issue 卡片网格：宽屏两列、窄屏一列。
- 每张卡片紧凑展示 Jira、Summary、Responsible、MR/Report/Handling 指标、Jira 状态及可用操作。
- 对尚未生成报告且具备权限的 Issue，不再显示重复的 `No report` 状态徽标，直接在卡片右上角显示 `Run Review`；顶部 No report 汇总数量继续保留。
- Preview 和 Run Review 都归入明确的卡片操作层级，避免 Status 列过窄和表格横向阅读负担。

## 7.2.4 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Sprint Overview 弹窗扩展到最高 1480px，并缩小视口边距；筛选区、统计指标和结果表格使用更紧凑的卡片层级与字段编排。
- Scan 增加阶段式活动反馈，依次展示 Jira 范围发现、MR 匹配、Review 活动核对和 Overview 汇总；使用不伪造百分比的连续加载动效，并支持 Reduced Motion。
- `No report` 行增加 `Run Review` 操作；仅对具备 Review 权限的用户显示，并复用现有单 Issue Review Job、报告复用确认、幂等提交和 Progress 跟踪流程。
- 结果表格设置合理的最小宽度和横向滚动边界，Status 操作区保持稳定，不挤压 Summary 与 Responsible。

## 7.2.3 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- `Run Release Gate` 从 URL 字段标题行移至 Release Gate 卡片右下角，URL 输入区域保持完整宽度。
- 校验和运行状态位于按钮左侧；窄屏下状态与按钮纵向排列，按钮使用完整宽度。

## 7.2.2 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- GIT_VERSION MR URL 输入框改为单行高度起步；只有内容在当前宽度放不下时才自然增长，最多显示两行。
- 输入、粘贴、Sprint 候选回填和窗口宽度变化都会重新计算输入框高度。
- 输入框隐藏垂直滚动条并禁用手动缩放，保持 Release Gate 卡片的紧凑布局。

## 7.2.1 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

- Release Gate 的前端和服务端统一使用严格的 GitLab MR URL 结构校验；复制的 URL 即使包含换行或首尾空白也会先规范化，避免有效链接被误判。
- GIT_VERSION MR URL 改为最多两行的自适应换行输入，完整 URL 可读性更好；提交前仍要求 `/group/project/-/merge_requests/<IID>` 完整路径。
- Sprint handoff 同时发现多个应用的 GIT_VERSION MR 时，显示“应用/分支”候选下拉框，选择后自动回填对应完整 URL，避免跨应用选错门禁。
- Review Progress 的 `Jump latest`、`Maximize/Restore` 改为带 Tooltip、键盘标签和触感反馈的标准图标按钮。
- Sprint Overview 将范围说明独占一行，Jira、Sprint、Filter 和 Scan 形成清晰的操作工具栏；补充平板和手机断点，避免 Grid 错位。
- 增加 URL 换行规范化、前后端校验一致性、Release Gate 两行输入和 Sprint Overview 布局契约测试。

## 7.2.0 — 2026-07-17

发布状态：已于 2026-07-17 部署到 `192.168.3.78:8765`；本机 `127.0.0.1:8765` 同步保留验收版本。

### 用户与权限管理

- Manager 可在 Web 中搜索和筛选用户，创建账户，维护 Developer/Auditor/Manager 角色、Active 状态及 responsible scope。
- 支持确认后重置密码；创建和重置产生的临时密码只在成功结果中显示，API 用户列表不暴露密码哈希。
- 所有登录用户可修改自己的密码；统一采用 14–128 位强密码策略。
- 停用、角色变更、密码重置与主动改密会撤销相关旧会话；禁止 Manager 自我降级/停用、停用受保护 root 或移除最后一个 Active Manager。

### 可靠性与验证

- `web_users.json` 使用进程内/跨进程锁、同目录原子替换和 `fsync` 持久化，损坏 JSON 采用 fail-closed 策略；记录级 revision 防止并发编辑静默覆盖。
- 新增独立脱敏用户审计日志，不记录临时密码、哈希或 Session。
- 全新用户库要求以 `WEB_BOOTSTRAP_ADMIN_PASSWORD` 安全引导首个 Manager，不再生成 `initial_credentials_*.txt` 明文凭据。
- 新增用户创建、脱敏列表、权限拒绝、重置及会话撤销、Manager/root 保护、旧 revision 兼容、审计失败回滚、禁用登录和损坏存储专项测试。
- Python 编译、内联 JavaScript 语法检查及完整 `182/182` 自动化测试通过；两个独立 Agent 完成 UI/可访问性与后端安全交叉验收。

### 生产部署与运行约束

- 生产环境从 7.0.3 升级至 7.2.0；RHEL9 完整测试 `182/182`、10 个账户 Credential Hash 完整性、登录页、健康接口、未认证权限保护和 GitLab 网络检查通过。
- 一致性备份位于 `/var/backups/codereviewer/7.0.3-to-7.2.0-20260717-203915`；使用 `sudo /usr/local/sbin/codereviewer-rollback-latest` 可一键恢复 7.0.3。
- 历史 `data/initial_credentials_20260714.txt` 已停止继续生成，生产文件权限已收紧为 `0600`；其中涉及账户仍应先完成统一密码轮换，再由获授权人员安全移除文件。
- 当前 User Management 按单 Web 实例运行。若后续启用多进程/多副本，用户变更、安全审计与 Idempotency-Key 需先迁移到同一 SQLite/专用数据库事务，不能仅依赖进程内幂等缓存。

## 7.1.7 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### 登录页必填标记

- Username、Password、Robot Check 使用统一的内联字段标题结构，红色 `*` 固定显示在字段名右侧。
- 修复 `label` 使用 Grid 时裸文本与必填标记被拆成两个 Grid Item，导致星号掉到下一行的问题。
- Robot Check 提示组支持窄屏自然换行，同时保持 Reset 按钮和问题输入框布局稳定。

### 验证

- 新增登录页必填标记 DOM/CSS 契约测试；完整自动化测试、Python 编译、内联 JavaScript 语法和本机健康检查通过。

## 7.1.6 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### Communication 与 Release Notes

- Follow-up Draft 使用固定尺寸 SVG copy icon，补充 hover、focus、active、成功和失败氛围状态；Clipboard API 与兼容回退保持不变，并通过 `aria-live` 通知结果。
- 删除 Communication 页面常驻的“处理说明”区块，将完整规则迁入 Reply 标题的 information hint；History、Follow-up Draft 和 Reply 三栏使用完整可用高度。
- Reply/Follow-up textarea 恢复纵向拖动，卡片按照内容自然增高，按钮始终处于 textarea 后方；右栏超高时内部滚动。
- Release Notes 使用专属受控弹窗，正文独立滚动，关闭按钮常驻；Escape、背景点击、焦点约束与关闭后焦点恢复均已补齐。

### Issues Review History

- 右侧概览指标调整为 Critical、High、Medium 和组合摘要四卡；三种问题级别都展示 handled/unhandled、百分比与进度条。
- Manager exceptions 与 Remaining blockers 合并为摘要卡，Remaining blockers 仍可跳转首个阻碍问题。
- 阻碍状态移至标题操作区，指标卡数字放在级别右侧；采用容器查询实现 4/2/1 列响应式布局。
- Overview 和 Issues 内容面板显式共享 Grid 第三行，避免概览区域被压缩成横向窄条。

### 验证

- 两个独立 Agent 分别完成交互/可访问性与布局/响应式只读审查，审查结论已纳入实现。
- Communication、Release Notes、Issue Review 指标与 Overview 专项测试以及完整自动化测试、Python 编译、内联 JavaScript 语法检查通过。

## 7.1.5 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### Review Communication、Report History 与报告预览

- Follow-up Draft 增加复制按钮，并提供 Clipboard API 与兼容性回退；无可复制内容时给出按钮内状态反馈。
- Reply 与 Follow-up 表单去除重复标题，两个输入区按右栏高度均分并自适应填充；处理说明缩小为辅助信息字号。
- Report History 的搜索框、日期/刷新行和 Tab 栏统一为完整宽度、相同 box model 与右侧边界，并与报告卡片操作区对齐。
- 报告“问题列表”按单个问题使用可访问的原生展开/折叠结构；默认展开，折叠时保留问题标题，表格和说明整体收起。

### 验证

- Review Communication、Report History、Markdown 报告 Tabs 与问题折叠专项回归通过。
- 完整自动化测试、Python 编译、内联 JavaScript 语法及本机服务健康检查通过。

## 7.1.4 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### Release Gate 紧凑布局

- GIT_VERSION Release Gate 从说明型大卡片调整为操作型紧凑卡片，移除与 information hint 重复的常驻说明段落。
- 左栏只保留 Release Workflow 步骤、标题和 Sprint handoff 状态；右栏将字段标题与 Run Release Gate 操作放在同一行，URL 输入框在下一行使用完整宽度。
- 重新校准项目卡片列宽、间距、背景和垂直节奏；窄屏继续按单列及全宽按钮显示。

### 验证

- Release Gate DOM/CSS 层级与紧凑布局验收通过。
- 完整自动化测试、Python 编译、内联 JavaScript 语法和差异检查通过。

## 7.1.3 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### 发布资源、项目门禁与 Web 体验

- Company Config/SCR 发布资源报告使用项目可读名称，命名为 `<Project>-Company Config_has-issue-<level>.md` 或 `<Project>-SCR_has-issue-<level>.md`；普通 Jira frontend/backend 报告命名保持兼容。
- GIT_VERSION Release Gate 根据 MR 所属 GitLab project 与 `config.yml` 强制匹配，分别归入 WVAdmin、iTrade Client、Services Terminal、DPS；未配置项目在进入 LLM 前拒绝。门禁元数据保留 project、project path、配置匹配和资源角色。
- 同项目、同资源类型和同严重级别的后续 Company Config/SCR 报告自动增加 rescan 时间戳，保留首份契约文件名且避免覆盖历史报告。
- `kelvinh.wu` 调整为 Auditor/Responsible，`benyq.feng` 归入其负责范围；`luckxh.chen` 负责 DPS Config 与 MO Client Config。
- Run Review 按“主操作、Release Gate、Progress”重整信息层级，Release Gate 和操作区在窄屏下自动切换单列；修复 Issues Review History 的 Overview/Issues 隐藏规则被 Grid 样式覆盖的问题。
- Review Communication 按 History、Follow-up Draft、Reply 三栏重新编排；处理说明横跨左两栏，Reply 与 Follow-up 操作卡在右栏纵向对齐。
- Pending Jira 采用 Jira ADF 作为稳定互操作格式；本地存在可用 bundle 时使用 Atlaskit Editor/Renderer 渐进增强，否则明确使用内置 ADF 编辑器，不把独立 Web fallback 标注为 Jira 原生组件。

### 验证

- 三名 Agent 分别完成发布项目规则、Run Review/Tab、Review Communication/Pending Jira 实现，并对最终合并状态交叉检查。
- 新增发布项目划分、Run Review 层级与 Tab 可见性、Review Communication 与 Pending Jira 渐进增强验收测试。
- 完整自动化测试 164 项通过；Python 编译、渲染后的内联 JavaScript 语法及差异检查通过。

## 7.1.2 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### Epic 生产验收收口

- Issues Review History 新增基于持久化 Sprint/Cycle 数据的 Overview/Issues Tab、Sprint 汇总卡和 History & Snapshots 关联视图。
- 同一 Issue 转入新 Sprint 时自动关闭旧 Cycle，并以 Issue 主记录关联全部 Sprint、Cycle、Run Group、Run 和 Snapshot。
- `all-findings-handled` 快照改为基于 Run Group 的 Handling 业务状态幂等，不再因不同 HTTP request key 重复生成。
- Sprint 候选限制为最近 31 天；Jira Sprint ID、访问权限和非空状态在提交前及服务端执行两次校验。
- Batch Issue Preview 必须明确确认；Final Sprint Review 模式由服务端重新计算并传入报告工作流元数据。
- 多 Job 最大化改为互斥；窄屏 Maximize/Restore 操作区响应式换行。
- Run Review、Release Gate、Coverage、Draft、Report Communication、Approval、Manager Exception 和 Login 补充 single-flight 或 in-flight 防重复保护。
- Problems、Issue History Discussion 与 Pending Jira ADF 使用统一必填提示、无障碍错误状态和首错聚焦；Jira 跟进描述必须由用户实际填写，不能提交空内容或示例文字。
### 验证

- 三名独立 Agent 分别执行 Problems UI、Cycle/Snapshot、Sprint/Progress 交叉审查。
- 新增 Epic 生产验收与 Problems UI 验收测试；完整自动化测试 148 项通过。
- Python 编译、渲染后的内联 JavaScript `node --check` 通过。

## 7.1.1 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### UI 与可用性改善

- Run Review 与 GIT_VERSION Release Gate 的说明统一为可点击、可键盘操作的 information icon，提示层支持点击外部和 `Esc` 关闭，并建立完整 ARIA 关联。
- 修复 Release Notes 调用不存在的 `markdownToHtml` 所导致的错误，统一复用页面 Markdown renderer。
- Report History 将搜索、周期、刷新和视图切换整合为工具卡；报告条目使用卡片、两行标题和元数据标签，改善窄栏信息层级。
- AI Assist 消息按用户/AI 角色呈现头像、时间、Markdown 正文和不同氛围；长 URL、列表及代码内容具备安全换行边界。
- Discussion 的 Reply 与 Follow-up 整理统一卡片结构、按钮尺寸和操作层级；处理说明横跨 History/Reply 两列。
- Issue Review 顶部重新组织为 Issue 身份、状态/操作、指标和门禁提示四个层级；长标题及 900px 以下布局可响应换行。

### 验证

- 独立 UI Agent 建立并执行 7 项语义/交互验收，最终 7/7 通过。
- 完整自动化测试：134 项通过。
- Python 编译与渲染后内联 JavaScript `node --check` 通过。

## 7.1.0 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### 新增与改善

- SQLite schema 升级为 v2，增加 Review Cycle、Sprint membership、Run Group、Description/Review Snapshot、Deferred Release Resource 和服务端幂等记录；旧数据以 Legacy/Backfilled 方式事务化迁移。
- 同一 Issue 跨 Sprint 再处理时，完整保留旧 Sprint/Cycle；MR 以 `project + IID + head SHA` 标识 revision，旧且未变化的 revision 可排除，新 Head SHA 会重新审核。
- LLM Prompt 分离 Current Review Scope、Current Target Context 和 Historical Requirement Context，确保旧 Cycle diff 不进入本轮审核对象。
- frontend/backend 报告归入同一逻辑 Run Group；Company Config/SCR 作为 deferred revision 等待 Web GIT_VERSION Release Gate。
- Problems 增加级别处理率、两行问题摘要、明确 lineage 文案、统一必填错误与可访问性；Not Issue 理由必填，Submit 文案收敛。
- Handling、Discussion、Pass 使用 350ms leading single-flight 与 Idempotency-Key；8 线程重复写入验收只生成一条业务记录。
- 最新 Run Group 全部 Finding 提交处理后自动生成不可变 Review Snapshot；审批、Manager Exception、Manual Pass 生成后续快照 revision。
- Sprint 输入支持近期历史搜索、Jira 有效性及 Development Done preflight，并区分 Batch Issue Preview 与 Final Sprint Review。
- Progress 支持用户滚动后暂停自动跟随、Jump to latest 和多 Job Maximize/Restore；Web Review 执行串行化，防止 Provider 环境变量跨任务污染。
- Report Preview 移除正式 Handling、Re-scan、Pass 与 Teams Delivery；正式闭环统一进入 Issue Review，AI Chat 更名为 AI Assist。
- 站点增加精简 `Release Notes` 弹窗，内容来自 `7.x-docs/public release notes.md`。
- POST 增加显式跨 Origin 拒绝、JSON 请求大小限制，并为 HTML/JSON 响应增加基础安全 Header。

### 验证

- 自动化单元、迁移、并发、服务边界与 Web 集成测试：127 项通过。
- Python 编译、渲染后内联 JavaScript `node --check`、`git diff --check` 通过。
- 本地服务仅监听 `127.0.0.1:8765`；未接触 3.78。

## 7.0.5 — 2026-07-17

发布状态：仅本地环境 `127.0.0.1:8765`；未部署或重启 `192.168.3.78:8765`。

### 新增与改善

- 增加 Manager 专用的 Web `GIT_VERSION Release Gate` 工作区，正常发布流程不再要求手动运行 CLI。
- Sprint Review Job 发现 GIT_VERSION MR 后提供 `Continue to Release Gate`，自动继承 Sprint 并预填 MR；多个候选通过输入候选列表选择。
- Release Gate 复用 Web Review Job 队列，支持进度、暂停、停止、Retry、页面重载后的任务恢复及在线报告预览。
- Release Gate Job 卡片展示 `READY/BLOCKED`、锁定源码仓库数、构建资源数和 blocker 数。
- 后端新增独立 `release-gate` 模式，只允许 Manager 调用，并校验完整 GitLab MR URL。
- 非 GIT_VERSION MR 或不包含版本化 `git_version.yml`/`build.yml` 的 MR 会在进入 LLM 前失败，避免误用模型和 Token。
- Company Config/SCR 继续从普通 Jira/Sprint Prompt 延后；GIT_VERSION Release Gate 继续以锁定源码/构建 commit 作为实际交付证据。
- CLI `python review.py --mr-url <GIT_VERSION MR>` 保留为自动化和故障排查备用入口。

### 验证

- 完整自动化测试：105 项通过。
- Manager 页面包含 Release Gate 工作区；Auditor/Developer 页面不渲染该入口。
- JavaScript 通过 Node 语法检查。
- 本地版本接口：`GET http://127.0.0.1:8765/api/version` 返回 `7.0.5`。

## 7.0.4 — 2026-07-16

发布状态：已发布到本地环境 `127.0.0.1:8765`；未部署到 `192.168.3.78`。

### 新增与改善

- 同一个 Jira Issue 同时涉及前端和后端 MR 时，报告按照 `responsible + project type` 分离问题清单。
- 支持从 `config.yml` 读取 `type: frontend` 和 `type: backend`。
- 支持两种配置结构：
  - CodeReviewer/Git Tools 扁平 repository 配置；
  - JiraReviewer 嵌套项目配置，repository 可继承上层项目的 `type` 和 `responsible`。
- 即使前端、后端属于同一负责人，也会生成相互独立的问题清单。
- 拆分报告文件名增加类型，例如：
  - `ECHNL-8888_frontend_has-issue-high.md`
  - `ECHNL-8888_backend_has-issue-critical.md`
- 报告 Basic Information 增加 `Project Type`，便于确认分类结果。
- 当前 `config.yml` 已补充 DPS、DPS build、iTrade Client、WVAdmin 等项目的前后端类型。

> 历史说明：以上是 7.0.4 当时的 project-type 拆分行为。7.2.12 已确认的目标边界为 Application Run：iTrade Client、Services Terminal、WVAdmin 分别拆分，DPS 保留 DPS9/DPS11 release line；详见 `CodeReviewer-Build-Repository-Knowledge.zh-CN.md`。该目标在完成代码升级前不能视为已实现。

### 使用说明

需要聚合同一 Jira Issue 的所有关联 MR，并按前后端拆分报告时，使用 Jira Review：

```powershell
python review.py --jira ECHNL-5657
```

`--mr-url` 仍只审核明确指定的单个 MR，不会自动扩展为整个 Jira Issue 的前后端 MR 集合。

### 验证

- 自动化测试：101 项通过。
- 当前配置解析：识别 51 个 repository，并能正确区分 frontend/backend。
- 本地版本接口：`GET http://127.0.0.1:8765/api/version` 返回 `7.0.4`。
- Git commit：`1c9913a fix: split Jira reports by frontend and backend`。

## 7.0.3 — 2026-07-16

发布状态：已部署到本地环境和 `192.168.3.78:8765`。

### 新增与改善

- 默认 LLM Provider 调整为 `codex-cli`，通过 CPA Responses API 执行审核。
- 禁止 CPA/Codex 失败后自动 fallback 到 CC Switch，避免额外等待以及不同 Provider 产生不一致报告。
- CC Switch 仅在显式配置 `LLM_PROVIDER=cc-switch` 时使用。
- DPS 项目发生 Codex 300 秒硬超时后，不再重复执行无效的同类请求。
- 完成 RHEL9 生产环境从 6.23.0 到 7.0.3 的升级，同时保留生产用户验证文件和既有数据。

### 验证

- 生产健康检查返回版本 `7.0.3`。
- CPA/Codex 连通性检查通过。
- 部署前 RHEL9 测试通过。

## 7.0.2 — 2026-07-16

### 新增与改善

- Windows 进程可读取用户级 `OPENAI_API_KEY`，改善旧 PowerShell/VS Code 会话看不到新环境变量的问题。
- 优化 Finding Handling 布局：提交按钮固定在问题标题右上角，普通处理不再保留无意义的空白右栏。
- “不是阻碍，另报 Jira”调整为更清晰的左右双栏布局，并提供 Jira Follow-up 卡片。
- 修正 `web-sv-build/dps` 等 DPS 根项目识别。
- 收敛 DPS Codex 超时后的重试和 Provider fallback 行为。

## 7.0.1 — 2026-07-16

### 新增与改善

- Windows 本地 CodeReviewer 默认通过 CLIProxyAPI `127.0.0.1:8318/v1` 调用 Codex Responses API。
- CPA API Key 由外部 `OPENAI_API_KEY` 注入，不写入 Git 仓库。
- 补充 Codex 300 秒超时诊断说明。
- 补充 Jira Issue、Sprint、Filter Review 的 MR 多来源发现顺序、范围过滤、分支类型路由及关闭 Sprint 后的诊断方法。

## 7.0.0 — 2026-07-15

### 新增与改善

- 建立 Developer、Auditor/Leader、Manager 三角色权限模型。
- 增加首批 Developer 账户及 responsible 映射。
- 增加 Issues Review History、Review Run、Finding lineage 和状态生命周期。
- 实现 `Handling → Re-scan → Problems → Handling` 闭环。
- Critical/High 的 Fixed 必须经过后续 Re-scan 验证。
- Developer 提交 Not an issue 后必须由 Auditor/Manager 审批。
- 增加 Manager Exception、Manual Pass 及审计记录。
- 增加 Report/Issue Discussion 和 AI Follow-up。
- 增加 Pending Jira 草稿及 ADF 可视化编辑、预览、版本控制和附件能力。
- ADF Expand 支持表格、有序列表、无序列表和截图。
- 增加 SQLite 工作流数据库、历史迁移及未来数据库适配边界。
- 密码存储升级为 PBKDF2-SHA256 Hash。
- 增加 GIT_VERSION Release Gate、锁定仓库检查和独立 LLM 上下文预算。
- 完成 Review Communication、Finding Handling、Issue Reviews、Tabs、卡片和安全间距等 Web 体验优化。

## 相关文档

- [CodeReviewer User Mannual](./CodeReviewer%20User%20Mannual.md)
- [CodeReviewer 7.0 Issue Workflow](./CodeReviewer-7.0-Issue-Workflow.md)
- [CodeReviewer 7.0 Issue Workflow 中文版](./CodeReviewer-7.0-Issue-Workflow.zh-CN.md)
